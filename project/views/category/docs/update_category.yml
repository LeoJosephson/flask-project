summary: Updates an existing category
parameters:
- in: path
  name: id
  description: category id to update
  required: true
  type: integer
- in: body
  name: category
  description: GamCategory to update.
  schema:
    type: object
    properties:
      name:
        type: string
      description:
        type: string
responses:
  200:
    description: OK
responses:
  200:
    description: OK
    schema:
      type: object
      properties:
        category:
          type: object
          properties:
            name:
              type: string
            description:
              type: string
  404:
    description: category not found
  400:
    description: validation Error